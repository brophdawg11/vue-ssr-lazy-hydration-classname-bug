Reproduction of a vue-lazy-hydration classname/attribute bug

## Install/Build

* Assuming `node@8` and `npm@5+`
* Clone this repository
* `npm install`
* `npm run dev` or `npm run prod`
* Open `http://localhost:8080/`
